/*
Template.favorito.items = function() {
  if (Session.equals('current_favorito', null))
    return null;
  else {
    var cats = favoritos.findOne({_id:Session.get('current_favorito')});
    if (cats && cats.items){
      for(var i = 0; i<ctas.items.length
*/      
    